{
    "include": [ // 타입스크립트 컴파일러에 포함 될 파일들을 include라는 키로 줄 수 있다.
        "src/**/*.ts" // src 폴더에 있는 모든 ts파일들을 포함시켰으니 컴파일 명령어에 파일명을 적을 필요가 없다.
    ],
    "exclude": [
        "node_modules" // 노드 패키지들은 타입스크립트 컴파일러 대상으로 제외 시켜줘야 한다.
    ],
    "compilerOptions": { // 컴파일러 옵션 지정
        "module":"es6", // es6 버전 모듈 사용
        "rootDir": "src", // 모듈에 루트가 되는 디렉토리를 src로 지정
        "outDir": "dist", // 컴파일 된 파일들이 만들어지는 최상위 디렉토리 지정
        "target": "es5", // 컴파일 시 es5로 변환
        "lib": ["ES6"],
        "sourceMap": true, // 브라우저 개발자 도구에서 트루로 지정하면 타입스크립트 코드도 볼 수 있다.
        "removeComments": true, // 트루로 지정하면 타입스크립트 파일을 컴파일해서 변환된 자바스크립트 코드에서는 주석이 없어진다.
        "noImplicitAny": true // 트루로 지정하면 변수 값이나 함수 파라미터 등에 어떠한 타입이 와야 되는지 지정을 안하면 오류를 내게 한다.

    }
}